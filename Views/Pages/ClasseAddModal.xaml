<UserControl x:Class="Taratra.Views.Pages.ClasseAddModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Taratra.Views.Pages" xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White"
        Padding="20"
        CornerRadius="10"
        Width="400">

        <StackPanel>

            <TextBlock Text="Ajout d'un nouveau classe"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0 0 0 20"/>


            <!-- FILTRE : CLASSE -->
            <StackPanel Margin="0 0 0 10" Orientation="Horizontal">
                <TextBlock Text="Classe" FontWeight="SemiBold" Margin="0 0 0 5"/>
                <TextBox Text="{Binding ClasseName}" Width="100" Margin="10 0 0 5"></TextBox>
            </StackPanel>
            <TextBlock Text="{Binding MessageError}" FontWeight="SemiBold" Margin="0 0 0 5" Foreground="red" HorizontalAlignment="Center"/>
            <!-- BOUTONS -->
            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0 20 0 0">

                <Button Content="Annuler"
    Width="100"
    Margin="0 0 10 0"
    Command="{Binding CloseModalCommand}"/>

                <Button Content="Ajouter"
    Width="100"
    Command="{Binding AddClasseCommand}"/>


            </StackPanel>

        </StackPanel>

    </Border>
</UserControl>
